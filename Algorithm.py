from omp import omp

LENGTH = 10

def oneIteration(qi, D):


def maxQuality():


def main():
    qVec = [0 for y in range(LENGTH)]
    D = [[0 for x in range(LENGTH)] for y in range(LENGTH)]
    while not maxQuality():
        oneIteration(qVec, D)


if __name__ == "__main__":
    main()
